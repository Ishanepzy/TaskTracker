@model TaskTracker.Controllers.AccountController

@{
    ViewData["Title"] = "Logout";
}

<h2>Logout</h2>

<form asp-action="Logout" method="post">
    <button type="submit" class="btn btn-danger">Logout</button>
</form>